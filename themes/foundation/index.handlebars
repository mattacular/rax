<!doctype html>
<html>
	<head>
		{{{htmlHead}}}
	</head>
	<body>
		{{#if options.showLogin}}
			{{{login}}}
		{{/if}}

		{{{contentHead}}}

		{{{hero}}}

		{{#if user.name}}
			<p style="color: #FFF;">Welcome {{user.name}}! <a href="/logout" id="signout">Signout</a></p> 
		{{/if}}


		{{{module_template}}}
	</body>
</html>